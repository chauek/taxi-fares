# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                  @controllers.Application.index
GET     /fare              @controllers.Application.startTracking
PUT     /fare/:fareId     @controllers.Application.addFarePoint(fareId: String)
